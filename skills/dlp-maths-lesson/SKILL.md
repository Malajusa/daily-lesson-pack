---
name: dlp-maths-lesson
description: Generate the main Daily Lesson Pack Mathematics teaching sequence using the universal Mathematics instruction canon, explicit mathematical language, verified representations and aligned independent evidence.
---

# DLP Mathematics Lesson

## Ownership

Own the main Mathematics lesson. Do not absorb the cumulative Mathematics warm-up into this sequence.

The Mathematics warm-up is retrieval. The main lesson owns prerequisite checking, explicit teaching, guided practice, independent application and exit evidence for the scheduled lesson focus.

## Mandatory references

Before planning or generating the lesson, read and apply:

- `references/universal-maths-instruction-canon.md`;
- `references/slide-deck-quality-standards.md`;
- `references/semantic-colour-standard.md`;
- `references/panel-containment-standard.md`.

The universal Mathematics canon is the authoritative pedagogical and mathematical-communication standard for this component. A current explicit user instruction may alter the lesson mode, slide count, context or representation, but it does not override mathematical accuracy, truthful representation or answer integrity.

## Internal planning contract

Before creating student-facing content, complete the planning contract required by the universal Mathematics canon:

1. curriculum concept and boundary;
2. lesson mode;
3. one central new idea, or `none` for review/assessment;
4. prerequisite knowledge to retrieve;
5. quantities, units, properties, structures or relationships students must notice;
6. what changes and what remains invariant;
7. technical vocabulary and notation;
8. selected representation or representations and why each is useful;
9. principal anticipated misconception;
10. highest independent demand;
11. aligned exit evidence.

Keep this planning record internal or teacher-facing. Do not place planning metadata on student slides.

## Topic integrity and scope

Identify the actual mathematical concept before choosing a representation. A number line, fraction wall, array, graph or diagram may support the topic but must not silently become the lesson topic.

For overview-controlled days, the weekly Mathematics topic/code and the day-level focus must agree with the authoritative Year 4/5 overview.

Unless the lesson is explicitly review, consolidation or extension, introduce one central new mathematical idea. Do not combine several substantial new concepts merely because they share a broad topic label.

Classify the lesson as one primary mode:

- concept introduction;
- procedure development;
- fluency development;
- reasoning;
- problem solving;
- review/assessment.

Use the mode to determine the balance of explanation, modelling, guidance and independent work. Do not apply an identical `I do / We do / You do` ratio to every lesson.

## Mathematics slide clarity standard

Every student-facing Mathematics task must communicate:

- the **action** students take;
- the **mathematical focus**;
- any required **representation/resource**;
- the expected **student output**.

Each slide must stand alone well enough that a relief teacher and an average student can determine what is required.

Avoid vague conversational shorthand. Do not use phrases such as `Does it settle it?`, `useful form`, `efficient proof` or similar wording that requires students to infer the mathematical decision. State the decision literally.

Use precise technical vocabulary with a plain-language explanation. Do not replace terms such as `factor`, `product`, `numerator`, `denominator`, `fractional unit`, `interval`, `axis`, `sample space`, `expression` or `equation` with vague substitutes when the technical term is required.

## Meaning before procedure

For any newly introduced method, establish the mathematical meaning before presenting procedural steps.

The explanation must identify the relevant quantities, units, relationships, what changes and what remains invariant. A mnemonic may support later recall but must not replace the explanation.

Do not make procedural shortcuts such as `borrow`, `move the decimal`, `keep the bottom number`, `flip and multiply`, `length times width` or `move it across and change the sign` the primary teaching explanation. Where such language is useful after meaning is established, connect it explicitly to the underlying mathematics.

## Teaching sequence

Keep cumulative retrieval, prerequisite checking and new teaching distinct.

For a new or fragile concept, use this default sequence:

1. focused prerequisite check;
2. explicit establishment of the concept and relationship;
3. one complete teacher model from start to finish;
4. immediate check for understanding;
5. unresolved `We do` / guided example;
6. guided practice with fading scaffolds;
7. independent application at a modelled demand;
8. reasoning, application or exit evidence measuring the intended concept.

Do not treat a token warm-up as prerequisite teaching.

During concept introduction or procedure development, students should make an observable response after no more than two consecutive explanation/model slides unless an explicit current instruction requires another structure.

A `We do` slide must require student participation. It must not already show the completed answer, final landing point, completed model or all working.

## Mathematical representations

Before drawing, define what the representation must prove or show and why that representation is suitable.

Verify all relevant quantities, labels, scales, intervals, partitions, coordinates, dimensions, geometric properties, graph values and sample-space outcomes.

A visual, verbal explanation and equation shown together must describe the same quantities, units and relationships. Make the correspondence explicit rather than assuming students will infer it.

Use the minimum number of representations needed to establish understanding. Each additional representation must add mathematical meaning.

Photographs may establish context but must not function as mathematical models unless their quantity, partition, scale and perspective are exact. Prefer flat, editable diagrams when decorative perspective or shadows could obscure equality, scale or correspondence.

When colour identifies corresponding quantities or meaningful changes, maintain the same colour relationship across the representation, language and notation. Do not rely on colour alone.

## Question and answer architecture

Task slides must not reveal the answer through completed working, answer-coloured objects, final landing points, completed models or teaching-only cues.

Answer slides must:

- answer the exact task;
- model the requested strategy or representation;
- show sufficient working or reasoning;
- fulfil every requested action such as explain, compare, justify, label, prove, draw or write an equation;
- acknowledge alternative valid methods or answers where relevant;
- place the primary answer in bold or otherwise visually dominant form;
- highlight meaningful mathematical changes rather than adding only a tick.

If the prompt asks students to explain with a model, a verbal answer without the model fails.

## Model-to-practice alignment

Compare the hardest independent task with the teaching sequence before release.

Independent work must not introduce an unmodelled conceptual demand. Check alignment in:

- concept;
- operation or relationship;
- representation;
- number type and magnitude;
- number of steps;
- strategy selection;
- language demand;
- reasoning demand;
- expected output.

When independent work includes more than one operation or method at a given level of complexity, model each at that complexity first unless one is explicitly labelled as extension.

## Variation, misconceptions and task types

Sequence examples using purposeful variation. Change one mathematically important feature at a time unless deliberately testing transfer.

Where a concept is defined by properties or boundaries, use a carefully selected example and non-example when this will clarify the distinction.

When including a misconception, use plausible student reasoning. The answer should identify the mistaken assumption, the mathematical property or relationship it violates, the correct reasoning and a supporting representation where useful.

Distinguish task type accurately:

- calculation applies a known process;
- reasoning explains, compares, justifies, classifies, generalises, proves or evaluates;
- problem solving requires students to determine useful mathematics and construct a pathway.

A routine worded calculation is not automatically problem solving.

## Established fraction requirements

For fraction comparison, explicitly teach strategy selection where relevant: same denominator, same numerator, equivalence, benchmark to one-half, distance from one, and mixed/improper representation. Explain when a method is inconclusive.

When teaching fraction operations, identify the fractional unit and explain how the numerator counts units. Do not make `add/subtract the top numbers and keep the bottom number` the primary explanation.

## Differentiation

Differentiate through representation, scaffold, reasoning, abstraction, strategy selection and context—not only by assigning more questions.

Do not define higher attainment as completing the same task with more working or checking. Where students choose between levels, each task must be a complete mathematical task at a different level of complexity rather than a different completeness standard for one question.

## Component acceptance result

Before returning the component for assembly, record `PASS` or `FAIL` and substantiate these checks:

- planning contract completed;
- lesson mode and central idea controlled;
- prerequisite retrieval distinct from the warm-up;
- meaning established before procedure;
- representation selected for a stated mathematical purpose;
- representation exactness verified;
- representation/language/notation correspondence verified;
- technical vocabulary verified;
- guided-practice answer withheld;
- hardest independent demand matched to a modelled precursor;
- answer slides fulfil every task requirement;
- exit evidence measures the intended concept;
- rendered readability, semantic colour and panel containment passed.

A missing check, unsupported `PASS` or unresolved defect is a component failure.

## QA

Fail the component if:

- instructions are ambiguous;
- lesson scope combines several unplanned new concepts;
- a procedure is taught without mathematical meaning;
- the representation changes the mathematical focus;
- a diagram or model is mathematically inaccurate;
- a decorative image is used as mathematical evidence;
- a visual, explanation and equation do not correspond;
- a `We do` or task slide reveals the answer;
- an answer does not model the requested method or representation;
- independent work introduces an unmodelled demand;
- technical vocabulary is vague or inaccurate;
- a misconception is implausible or diagnostically useless;
- a task labelled reasoning/problem solving is only routine calculation;
- the exit check measures only the representation rather than the concept;
- a valid alternative answer is rejected without a stated restriction;
- the rendered slide is unreadable, visually misleading or poorly contained.

Use `examples/benchmarks/universal-maths-canon-regression.md` whenever a change affects Mathematics pedagogy, representations, task architecture or QA. Test at least two different concept families and fail the change if improvement in one area breaks another.
