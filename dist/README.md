# Historical distributions

The existing ZIPs in this directory predate 3.10.0-rc.1. They are retained as
historical artefacts, not silently relabelled as the current candidate. Build a
new package from accepted source into a separate output directory, verify its
manifest and external digest, and identify the actual activated host separately.
The candidate CI run publishes the exact tested complete/component packages.
